public class CaseCloneWithFilesHandler {
    public static void getCaseWithFiles(List<Case>getCase){
        for(Case cs : getCase){
            if(cs.Status == 'Closed' && cs.ContactId != 'False'){
             }
            List<ContentDocumentLink>cdlList = new List<ContentDocumentLink>();
        }
        
    }
}